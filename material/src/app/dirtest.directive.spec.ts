import { DirtestDirective } from './dirtest.directive';

describe('DirtestDirective', () => {
  it('should create an instance', () => {
    const directive = new DirtestDirective();
    expect(directive).toBeTruthy();
  });
});
